import { TitlesStyleDirective } from './titles-style.directive';

describe('TitlesStyleDirective', () => {
  it('should create an instance', () => {
    const directive = new TitlesStyleDirective();
    expect(directive).toBeTruthy();
  });
});
